const emoji = "😊"; // 😊
const codeUnits = emoji.split("");
const unicodeEscapes = codeUnits.map((codeUnit) => {
  return "\\u" + codeUnit.charCodeAt(0).toString(16);
});
const twoUnitUnicode = unicodeEscapes.join("");

console.log(twoUnitUnicode); // \uD83D\uDE0A
